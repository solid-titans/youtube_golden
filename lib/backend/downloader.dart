import 'package:flutter/material.dart';

class Downloader extends StatelessWidget {
  const Downloader({Key? key}) : super(key: key);

  static const routeName = "backend/downloader";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
